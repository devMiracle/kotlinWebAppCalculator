package controllers

fun add(first : Double, second : Double) : Number{
    return first + second
}
fun substract(first : Double, second : Double) : Number{
    return first - second
}
fun multiply(first : Double, second : Double) : Number{
    return first * second
}
fun divide(first : Double, second : Double) : Number{
    return first / second
}